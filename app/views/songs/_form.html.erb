<%= form_for @song do |f| %>

<div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
<div class="field">
    <%= f.label :genre %>
    <%= f.text_field :genre %>
    </div>
<div class="field">
    <%= f.label :duration %>
    <%= f.number_field :duration %>
    </div>
    <div class="field">
    <%= f.label :poster %><br>
    <%= f.file_field :avatar %>
    </div>



    <div class="well">
        <h3>Artist</h3>
        <%= f.fields_for(:artist) do |a| %>
      
          <%= f.select :artist_id, options_for_select(@artist.map{ |s| [s.first_name + s.last_name, s.id] }) %>
        <% end %>
      </div>








    <%= f.submit (@song.new_record? ? "Create" : "Update") %>

<% end %>
